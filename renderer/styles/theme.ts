import { Theme } from "@emotion/react";

const palette = {
  disabled: '#d5d5d5',
  darkgray: '#5c5c5c',
  border: '#dbdbdb',
  white: '#ffffff',
  whitesmoke: '#f5f5f5',
  black: '#000000',
};

const theme: Theme = {
  palette,
};

export default theme;
